describe('testing', function () {
    it('should to be true', function () {
        expect(true).to.be.equal(true);
    });
    it('should to be true', function () {
        expect(false).to.be.equal(false);
    });
});